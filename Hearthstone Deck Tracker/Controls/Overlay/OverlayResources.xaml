<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:Hearthstone_Deck_Tracker.Utility.Converters"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    >
    <SolidColorBrush x:Key="Tier7Purple" Color="#361637"/>
    <SolidColorBrush x:Key="Orange" Color="#FFB00D"/>
    <SolidColorBrush x:Key="Black" Color="#141617"/>
    <SolidColorBrush x:Key="Highlight" Color="#6A9D36"/>
    <SolidColorBrush x:Key="YellowButtonBackground" Color="#F1C040"/>
    <SolidColorBrush x:Key="YellowButtonBackgroundHover" Color="#CCCCCC"/>
    <SolidColorBrush x:Key="YellowButtonForeground" Color="#26200F"/>

    <Style x:Key="SubscribeButton" TargetType="{x:Type ButtonBase}">
        <Setter Property="Background" Value="{StaticResource YellowButtonBackground}" />
        <Setter Property="Foreground" Value="{StaticResource YellowButtonForeground}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ButtonBase}">
                    <ControlTemplate.Triggers>
                        <Trigger Property="UIElement.IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{StaticResource YellowButtonBackgroundHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter Content="{TemplateBinding Content}" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
